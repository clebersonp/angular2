var num = 2;

var n: string = "5";
n = "abc";

var numeros = [1, 2, 3, 4];

numeros.map( n => n * 2 );

class Matematica {
    somar(x, y) {
        return x + y;
    }
}

let variavel: string = "valor";

const PI: number = 3.1415;

class Cat {
    public name: string = "Mel";
    public meaow() {
        return `${this.name} say Meaowwwww!`;
    }    
}
